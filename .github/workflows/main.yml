name: 🚀 Deploy website on push

on:
  push:
    branches: [v3]
  pull_request:
    branches: [v3]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [16.x]

    steps:
      - name: 🚚 Checkout repository
        uses: actions/checkout@v2

      - name: 🔨 Build Project
        run: |
          yarn
          yarn build

      - name: List output files
        run: find out/ -print

      - name: 📂 Sync files
        uses: SamKirkland/FTP-Deploy-Action@4.2.0
        with:
          server: 141.95.193.28
          username: flamboyant-bohr_i9ndl2am7r
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: out/
